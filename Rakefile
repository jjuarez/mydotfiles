# frozen_string_literal: true

unless ENV['DOTFILES']
  warn("💣 Environment variable DOTFILES undefined, setting it to: #{Dir.pwd}")
  ENV['DOTFILES'] = Dir.pwd
end

task :default do
  puts("Just try 'rake --tasks'")
end
